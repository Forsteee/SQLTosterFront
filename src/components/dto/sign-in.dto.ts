import {observable} from "mobx";
import {Length} from "class-validator";

export class SignInDto{
   /* @Length(3,100)
    @observable
    username: string;

    @Length(6,100)
    @observable
    password: string;*/

}